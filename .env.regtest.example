SWAP_NETWORK=regtest
LN_BACKEND=fake
ARK_MNEMONIC="<generate with any bip39 tool>"
ARK_SERVER_URL=http://localhost:7070
EMULATOR_URL=http://localhost:7073
ARK_DB_PATH=.data/ark-regtest.sqlite
SWAP_DB_PATH=.data/swaps-regtest.sqlite
FAKE_LN_STATE_PATH=.data/fake-ln-regtest.json

# Admin console. Off unless ADMIN_PORT is set.
# THERE IS NO AUTH ON THIS PORT — anything that reaches it can move money.
# Keep the default loopback bind and tunnel in; see docs/runbook.md.
# ADMIN_PORT=8788
# ADMIN_HOST=127.0.0.1
