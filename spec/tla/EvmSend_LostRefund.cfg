\* EVM send corridor, MUTATION: ResendUnrecordedRefund (A8) = FALSE.  Deletes
\* the shipped resend, leaving the behaviour the corridor USED TO have: a row
\* that CASed into refunding_evm and then never got its broadcast away - the
\* send threw, or a crash landed before the txid patch
\* (evmOrchestrator.ts:380-384) - waits forever on a receipt for a transaction
\* that does not exist.  `pending` there is ignorance, not patience, and
\* nothing retried.
\*
\* THIS IS F4'S EXPENSIVE HALF, REACHED THE WAY THE CODE CAN REACH IT.  With
\* no refund in flight the lock stays present and claimable; the client takes
\* the ERC20 and, at the wall locktime, its own sats back.  The margin cannot
\* help, because the margin is a budget for a refund that was SENT.
\*
\* The shipped planner now sends one over a present lock past its timeout
\* (evmSendPlan.ts:176-182), so this deletes a shipped guard rather than
\* recording shipped behaviour.
\*
\* EXPECTED RESULT: invariant NoNetLoss violated.  Liveness is not checked
\* here; TLC stops at the invariant violation.

\* RESULT: fires as expected.  See the module's MUTATION CHECKS for counts.

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 5
    Amount     = 1
    MaxExposed = 2
    Phases     <- ESPhases
    Results    <- ESResults
    SpendKinds <- ESSpendKinds

    EvmTimeoutH    = 1
    RefundLocktime = 4
    ValidUntil     = 2
    MaxHeight      = 4
    FastCad        = 1
    SlowCad        = 2

    LockCommitFirst    = TRUE
    TxidPatchAtomic    = TRUE

    ScanFollowsTheRow  = TRUE
    RefundSeesClaim    = TRUE
    ResendUnrecordedRefund     = FALSE
    LockLandsPromptly  = TRUE
    TimeoutRefundCoversPresent = TRUE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleLock
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane
