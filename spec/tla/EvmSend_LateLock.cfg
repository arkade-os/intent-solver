\* EVM send corridor, MUTATION: LockLandsPromptly (A6) = FALSE - the shipped
\* truth for a pending transaction.  A lock broadcast can in principle land
\* at ANY later height; if it lands at or after the timeout height, the row
\* may already be `refunded` (terminal, recorded over the never-confirmed
\* lock) when the ERC20 actually leaves - F4.
\*
\* EXPECTED RESULT: invariant NoNetLoss violated - the client holds the
\* late-landing ERC20 AND its own refunded sats.  Liveness is not checked
\* here; TLC stops at the invariant violation.
\*
\* THE INVARIANT CHANGED WITH THE (A4) FIX, and F4 did not go away with it.
\* This cfg used to fire NoSilentLoss at depth 15: the cheaper witness was a
\* terminal `refunded` row recorded over a lock that had not landed yet, and
\* recording `refunded` on the strength of the broadcast is exactly what (A4)
\* stopped.  Verified rather than assumed - run this cfg against the module at
\* df62911 and NoSilentLoss still falls at depth 15 (58,327/10,950 at that revision).  What is
\* left is the expensive half: the lock lands late, the client claims it and
\* still refunds its sats.  Only F4 itself closes that.

\* RESULT: fires as expected.  691,921 states generated, 113,703 distinct, depth 21, 3s (-workers 4).

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 5
    Amount     = 1
    MaxExposed = 2
    Phases     <- ESPhases
    Results    <- ESResults
    SpendKinds <- ESSpendKinds

    EvmTimeoutH    = 1
    RefundLocktime = 4
    ValidUntil     = 2
    MaxHeight      = 4
    FastCad        = 1
    SlowCad        = 2

    LockCommitFirst    = TRUE
    TxidPatchAtomic    = TRUE

    ScanFollowsTheRow  = TRUE
    RefundSeesClaim    = TRUE
    LockLandsPromptly  = FALSE
    TimeoutRefundCoversPresent = TRUE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleLock
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane
