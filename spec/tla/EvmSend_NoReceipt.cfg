\* EVM send corridor, MUTATION: RefundSeesClaim (A4) = FALSE - the shipped
\* truth.  The row records `refunded` at SEND time with no receipt check
\* (broadcast.ts:91-95): if the client's claim lands before the refund
\* mines, the broadcast reverts and the row still says `refunded` -
\* terminal, and lying about where the ERC20 went.
\*
\* EXPECTED RESULT: invariant NoSilentLoss violated (a terminal `refunded`
\* row over a client-claimed lock).  Liveness is not checked here; TLC
\* stops at the invariant violation.

\* RESULT: fires as expected.  58,614 states generated, 10,991 distinct, depth 15, 0s (-workers 4).

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 5
    Amount     = 1
    MaxExposed = 2
    Phases     <- ESPhases
    Results    <- ESResults
    SpendKinds <- ESSpendKinds

    EvmTimeoutH    = 1
    RefundLocktime = 4
    ValidUntil     = 2
    MaxHeight      = 4
    FastCad        = 1
    SlowCad        = 2

    LockCommitFirst    = TRUE
    TxidPatchAtomic    = TRUE
    RefundSeesClaim    = FALSE
    LockLandsPromptly  = TRUE
    TimeoutRefundCoversPresent = TRUE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleLock
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane
