\* EVM receive corridor, MUTATION: RefundSpendAtomic (B5) = FALSE - the
\* refund CAS and the covenant spend split, and a crash between them
\* strands the row in refunding_arkade with the covenant UNSPENT (the
\* planner never re-drives it, plan:126-127).  The patient client waits
\* for its own timeout, claims the sats, and takes its ERC20 back - F4,
\* loudly stuck and still a double loss.
\*
\* EXPECTED RESULT: invariant NoNetLoss violated.  Liveness is not checked
\* here; TLC stops at the invariant violation.

\* RESULT: fires as expected.  781,930 states generated, 120,211 distinct, depth 26, 15s (-workers 4).

SPECIFICATION Spec

\* SYMMETRY IS DELIBERATELY NOT USED (SwapCore trap T4).

\* The model deliberately goes quiescent once every swap is terminal (or
\* crash-parked) and both clocks have reached their bound; that is the end
\* of the modelled world, not a stuck system.  Liveness is what checks for
\* real stalls.
CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 4
    Amount     = 1
    MaxExposed = 2
    Phases     <- ERPhases
    Results    <- ERResults
    SpendKinds <- ERSpendKinds

    EvmTimeoutH       = 3
    ClaimMarginBlocks = 1
    RefundLocktime    = 2
    ValidUntil        = 2
    MaxHeight         = 5
    FastCad           = 1
    SlowCad           = 2

    FundCommitFirst    = TRUE
    ClaimLandsPromptly = TRUE
    ClaimRecordsReceipt = TRUE
    FundLandsPromptly  = TRUE
    RefundSpendAtomic  = FALSE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleFund
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane

PROPERTIES
    ForwardOnly
    Liveness
