\* EVM receive corridor, MUTATION: RefundRedrivenWhileUnspent (B5) = FALSE,
\* restoring the parked refunding_arkade the planner USED TO have, over
\* RefundSpendAtomic = FALSE, which is the shipped three-step shell.  A crash
\* between the refund CAS and the covenant spend then strands the row with
\* the covenant UNSPENT and nothing to notice: the patient client waits for
\* its own timeout, claims the sats, and takes its ERC20 back - F4, loudly
\* stuck and still a double loss.
\*
\* The split is not the bug - EvmReceive_LostSpend.cfg is this cfg with the
\* shipped re-drive, and it is GREEN over the same crash window.  The park is.
\*
\* EXPECTED RESULT: invariant NoNetLoss violated.  Liveness is not checked
\* here; TLC stops at the invariant violation.

\* RESULT: fires as expected.  See the module's MUTATION CHECKS for counts.

SPECIFICATION Spec

\* SYMMETRY IS DELIBERATELY NOT USED (SwapCore trap T4).

\* The model deliberately goes quiescent once every swap is terminal (or
\* crash-parked) and both clocks have reached their bound; that is the end
\* of the modelled world, not a stuck system.  Liveness is what checks for
\* real stalls.
CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 4
    Amount     = 1
    MaxExposed = 2
    Phases     <- ERPhases
    Results    <- ERResults
    SpendKinds <- ERSpendKinds

    EvmTimeoutH       = 3
    ClaimMarginBlocks = 1
    RefundLocktime    = 2
    ValidUntil        = 2
    MaxHeight         = 5
    FastCad           = 1
    SlowCad           = 2

    FundCommitFirst    = TRUE
    ClaimLandsPromptly = TRUE
    ClaimRecordsReceipt = TRUE
    FundLandsPromptly  = TRUE
    RefundSpendAtomic  = FALSE

    RefundRedrivenWhileUnspent = FALSE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleFund
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane

PROPERTIES
    ForwardOnly
    Liveness
