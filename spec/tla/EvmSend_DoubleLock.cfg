\* EVM send corridor, MUTATION: LockCommitFirst (A1) = FALSE.
\*
\* The shipped-order question: the CAS quoted->locking_evm commits the row
\* BEFORE the lock broadcast (evmOrchestrator.ts:281).  With the order
\* flipped, two workers can both read quoted+funded and both broadcast the
\* ERC20 lock before either CAS lands - the double-lock.
\*
\* EXPECTED RESULT: invariant NoDoubleLock violated (lockSends reaches 2).
\* RESULT: fires as expected.  3,135 states generated, 663 distinct, depth 9, 0s (-workers 4).
\* Liveness is not checked here; TLC stops at the invariant violation.

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 5
    Amount     = 1
    MaxExposed = 2
    Phases     <- ESPhases
    Results    <- ESResults
    SpendKinds <- ESSpendKinds

    EvmTimeoutH    = 1
    RefundLocktime = 4
    ValidUntil     = 2
    MaxHeight      = 4
    FastCad        = 1
    SlowCad        = 2

    LockCommitFirst    = FALSE
    TxidPatchAtomic    = TRUE

    ScanFollowsTheRow  = TRUE
    RefundSeesClaim    = TRUE
    LockLandsPromptly  = TRUE
    TimeoutRefundCoversPresent = TRUE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleLock
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane
