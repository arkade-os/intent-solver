\* EVM send corridor, MUTATION: BreakDeadlineOrder (A2) = TRUE - the margin
\* removed by fiat: the wall locktime no longer trails the height timeout
\* at the slowest cadence (EvmTimeoutH 1 * SlowCad 2 = RefundLocktime 2,
\* margin 2 < 2 FALSE).  The client claims the ERC20 at the timeout moment
\* and refunds its sats in the same breath; the solver's refund has no
\* budget to land in.
\*
\* EXPECTED RESULT: invariant NoNetLoss violated.  The green cfg is the
\* control (margin intact, green).  Liveness is not checked here; TLC
\* stops at the invariant violation.

\* RESULT: fires as expected.  46,851 states generated, 8,929 distinct, depth 14, 0s (-workers 4).

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 5
    Amount     = 1
    MaxExposed = 2
    Phases     <- ESPhases
    Results    <- ESResults
    SpendKinds <- ESSpendKinds

    EvmTimeoutH    = 1
    RefundLocktime = 2
    ValidUntil     = 2
    MaxHeight      = 4
    FastCad        = 1
    SlowCad        = 2

    LockCommitFirst    = TRUE
    TxidPatchAtomic    = TRUE
    RefundSeesClaim    = TRUE
    LockLandsPromptly  = TRUE
    TimeoutRefundCoversPresent = TRUE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = TRUE

INVARIANTS
    TypeOK
    NoDoubleLock
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane
