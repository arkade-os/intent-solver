\* EVM receive corridor, MUTATION: FundLandsPromptly (B4) = FALSE - a hung
\* funding RPC.  The fund CAS happens inside the RULE 2 window, but the
\* accept lands only at or after the client's timeout height: the client
\* refunds its ERC20 and claims the freshly-landed sats in the same
\* breath - F3.
\*
\* EXPECTED RESULT: invariant NoNetLoss violated.  Liveness is not checked
\* here; TLC stops at the invariant violation.

\* RESULT: fires as expected.  268,386 states generated, 43,170 distinct, depth 21, 7s (-workers 4).

SPECIFICATION Spec

\* SYMMETRY IS DELIBERATELY NOT USED (SwapCore trap T4).

\* The model deliberately goes quiescent once every swap is terminal (or
\* crash-parked) and both clocks have reached their bound; that is the end
\* of the modelled world, not a stuck system.  Liveness is what checks for
\* real stalls.
CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 4
    Amount     = 1
    MaxExposed = 2
    Phases     <- ERPhases
    Results    <- ERResults
    SpendKinds <- ERSpendKinds

    EvmTimeoutH       = 3
    ClaimMarginBlocks = 1
    RefundLocktime    = 2
    ValidUntil        = 2
    MaxHeight         = 5
    FastCad           = 1
    SlowCad           = 2

    FundCommitFirst    = TRUE
    ClaimLandsPromptly = TRUE
    ClaimRecordsReceipt = TRUE
    FundLandsPromptly  = FALSE
    RefundSpendAtomic  = TRUE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleFund
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane

PROPERTIES
    ForwardOnly
    Liveness
