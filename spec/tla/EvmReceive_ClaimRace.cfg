\* EVM receive corridor, MUTATION: ClaimLandsPromptly (B2) = FALSE.
\*
\* A broadcast claimCall no longer has to mine before the height reaches
\* the client's timeout.  The client's ERC20 refund opens first, the claim
\* reverts, and the client holds both the sats (its Arkade claim is what
\* revealed the preimage) and its tokens back.  The 60-block claim margin
\* of RULE 2 is the shipped headroom for exactly this race.
\*
\* EXPECTED RESULT: invariant NoNetLoss violated.  Liveness is not checked
\* here; TLC stops at the invariant violation.

\* RESULT: fires as expected.  762,788 states generated, 117,273 distinct, depth 26, 16s (-workers 4).

SPECIFICATION Spec

\* SYMMETRY IS DELIBERATELY NOT USED (SwapCore trap T4).

\* The model deliberately goes quiescent once every swap is terminal (or
\* crash-parked) and both clocks have reached their bound; that is the end
\* of the modelled world, not a stuck system.  Liveness is what checks for
\* real stalls.
CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 4
    Amount     = 1
    MaxExposed = 2
    Phases     <- ERPhases
    Results    <- ERResults
    SpendKinds <- ERSpendKinds

    EvmTimeoutH       = 3
    ClaimMarginBlocks = 1
    RefundLocktime    = 2
    ValidUntil        = 2
    MaxHeight         = 5
    FastCad           = 1
    SlowCad           = 2

    FundCommitFirst    = TRUE
    ClaimLandsPromptly = FALSE
    ClaimRecordsReceipt = TRUE
    FundLandsPromptly  = TRUE
    RefundSpendAtomic  = TRUE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleFund
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane

PROPERTIES
    ForwardOnly
    Liveness
