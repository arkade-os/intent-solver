\* EVM receive corridor, MUTATION: FundCommitFirst (B1) = FALSE.
\*
\* The funding-order question: the row CASes into funding_arkade BEFORE
\* the sats go out (evmOrchestrator.ts:230-237).  With the order flipped,
\* two workers can both read quoted+locked and both fund the Arkade
\* lockup before either CAS lands - the double fund.
\*
\* EXPECTED RESULT: invariant NoDoubleFund violated (fundSends reaches 2).
\* RESULT: fires as expected.  3,223 states generated, 725 distinct, depth 9, 0s (-workers 4).
\* Liveness is not checked here; TLC stops at the invariant violation.

SPECIFICATION Spec

\* SYMMETRY IS DELIBERATELY NOT USED (SwapCore trap T4).

\* The model deliberately goes quiescent once every swap is terminal (or
\* crash-parked) and both clocks have reached their bound; that is the end
\* of the modelled world, not a stuck system.  Liveness is what checks for
\* real stalls.
CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 4
    Amount     = 1
    MaxExposed = 2
    Phases     <- ERPhases
    Results    <- ERResults
    SpendKinds <- ERSpendKinds

    EvmTimeoutH       = 3
    ClaimMarginBlocks = 1
    RefundLocktime    = 2
    ValidUntil        = 2
    MaxHeight         = 5
    FastCad           = 1
    SlowCad           = 2

    FundCommitFirst    = FALSE
    ClaimLandsPromptly = TRUE
    ClaimRecordsReceipt = TRUE
    FundLandsPromptly  = TRUE
    RefundSpendAtomic  = TRUE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleFund
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane

PROPERTIES
    ForwardOnly
    Liveness
