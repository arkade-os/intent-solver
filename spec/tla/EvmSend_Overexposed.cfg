\* EVM send corridor, MUTATION: AtomicAdmission = FALSE.
\*
\* quote()'s INSERT re-checks the exposure cap in the same breath as the
\* read (the reservation lease of evmOrchestrator.ts:435-451).  Dropping
\* the re-check lets two admissions both pass the cap.  MaxExposed = 1 so
\* the second admission is the overflow (the green cfg's 2 would admit
\* both legitimately).
\*
\* EXPECTED RESULT: invariant ExposureBounded violated.  The green cfg is
\* the control (AtomicAdmission = TRUE; its MaxExposed = 2 admits both).
\* RESULT: fires as expected.  1,056 states generated, 274 distinct, depth 7, 0s (-workers 4).
\* Liveness is not checked here; TLC stops at the invariant violation.

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 5
    Amount     = 1
    MaxExposed = 1
    Phases     <- ESPhases
    Results    <- ESResults
    SpendKinds <- ESSpendKinds

    EvmTimeoutH    = 1
    RefundLocktime = 4
    ValidUntil     = 2
    MaxHeight      = 4
    FastCad        = 1
    SlowCad        = 2

    LockCommitFirst    = TRUE
    TxidPatchAtomic    = TRUE

    ScanFollowsTheRow  = TRUE
    RefundSeesClaim    = TRUE
    ResendUnrecordedRefund     = TRUE
    LockLandsPromptly  = TRUE
    TimeoutRefundCoversPresent = TRUE
    AtomicAdmission    = FALSE
    BreakDeadlineOrder = FALSE

INVARIANTS
    ExposureBounded
