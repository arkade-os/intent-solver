\* EVM send corridor, MUTATION: TimeoutRefundCoversPresent (A7) = FALSE -
\* the shipped truth.  The locking_evm timeout refund fires only when the
\* lock is ABSENT (evmSendPlan.ts:118-119).  A lock that is present but
\* never proven deep (the depth probe lies / the node stalls: lockDepth.ts
\* treats a failed read as UNPROVEN, never absent) parks the row past its
\* own timeout.  The client then claims the ERC20 at any height - the
\* contract does not gate the claim on the timeout - and refunds its sats
\* at the wall locktime.  The double loss the margin exists to prevent,
\* delivered through the one branch that withholds the timeout.
\*
\* EXPECTED RESULT: invariant NoNetLoss violated.  Liveness is not checked
\* here; TLC stops at the invariant violation.

\* RESULT: fires as expected.  270,602 states generated, 46,905 distinct, depth 18, 1s (-workers 4).

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Swaps   = {s1, s2}
    Workers = {w1, w2}
    NoSwap  = NoSwap
    NoState = NoState
    MaxClock   = 5
    Amount     = 1
    MaxExposed = 2
    Phases     <- ESPhases
    Results    <- ESResults
    SpendKinds <- ESSpendKinds

    EvmTimeoutH    = 1
    RefundLocktime = 4
    ValidUntil     = 2
    MaxHeight      = 4
    FastCad        = 1
    SlowCad        = 2

    LockCommitFirst    = TRUE
    TxidPatchAtomic    = TRUE
    RefundSeesClaim    = TRUE
    LockLandsPromptly  = TRUE
    TimeoutRefundCoversPresent = FALSE
    AtomicAdmission    = TRUE
    BreakDeadlineOrder = FALSE

INVARIANTS
    TypeOK
    NoDoubleLock
    AtMostOneOutcome
    ExposureBounded
    NoSilentLoss
    NoNetLoss
    RefusedUnreachableFromExposed
    StuckReachableFromEveryExposed
    HeightSane
